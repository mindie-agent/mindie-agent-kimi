---
description: reporting-status for independent product failure reporting
---

Call `mindie_entry` once with `op=reporting-status` and a fresh `request_nonce`. The current native command authorizes this operation; never pass a session id or infer authorization from model arguments. Do not start a reporter, retry, activate knowledge, or change community contribution. Show the returned shared reporting state.
$ARGUMENTS
