---
description: Initialize MindIE Agent for this native Kimi session, or show status if already configured
---

A native TurnStarted hook binds `/mindie-agent:init` from this session's plugin_command origin on the wire. Do not pass a session id, do not guess the newest session, and do not treat quoted command text as activation. After native bind, knowledge tools need a fresh request_nonce. If unconfigured, explain the three sharing choices (recommended public contribution, read-only, later).
$ARGUMENTS
