# P0/P1 follow-up evidence

The organizer records actual process and observations, omits unspecified
details, and does not synthesize lessons, execution history or a causal
conclusion. Kimi 2.0.2/K3 max produced one faithful record from an actual NPU
task's public output in 53.97 seconds. Separately, a real K3 continuation from
a compacted GitHub PR receipt selected the original entry ID from normal
header context, restored its exact remote body and appended in 30.33 seconds.
That overlapping review material stayed local; it was not published again.

Actual feed success mixed with a GitHub 404 reports `degraded`; a real lock
conflict reports `deferred`. Previous content/references survive, and recovery
clears the error. There were no automatic retries or model calls for sync.

A real native fork made one knowledge query and was refused because it had
not explicitly activated the plugin. It did not inherit the parent's lease
or create capture/publication state.

Real launchd acceptance exposed registration selecting a retained, stale
bootstrap launcher that rejected `--config`. Registration now selects the
current complete generation. The corrected production function, registered
under an isolated label with a 30-second test interval, completed its first
OS dispatch in 34.49 seconds, runs=1/exit=0, using installed main 421e22b.
All test labels were removed. This timer result is distinct from the new
feed-result implementation's real-network checks.

Final contribution/Bot/new-task integration and Windows hardware acceptance
remain separate from these scoped native results. Old business Skills and
profiling are deferred.
